<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/SplashBackground"
    tools:context=".Activities.SplashActivity">

    <ImageView
        android:id="@+id/logo_left"
        android:layout_width="34.2857142857dp"
        android:layout_height="90dp"
        android:contentDescription="@string/app_name"
        android:src="@drawable/ic_logo_left"
        app:layout_constraintBottom_toBottomOf="@+id/logo_bell"
        app:layout_constraintEnd_toStartOf="@+id/logo_bell"
        app:layout_constraintTop_toTopOf="@+id/logo_bell" />

    <ImageView
        android:id="@+id/logo_bell"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:contentDescription="@string/app_name"
        android:padding="10dp"
        android:src="@drawable/ic_logo_bell"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/logo_right"
        android:layout_width="34.2857142857dp"
        android:layout_height="90dp"
        android:contentDescription="@string/app_name"
        android:src="@drawable/ic_logo_right"
        app:layout_constraintBottom_toBottomOf="@+id/logo_bell"
        app:layout_constraintStart_toEndOf="@+id/logo_bell"
        app:layout_constraintTop_toTopOf="@+id/logo_bell" />

    <ProgressBar
        android:id="@+id/dpSlashProgressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminateTint="@color/appBlueColor"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/logo_bell"
        app:layout_constraintVertical_bias="0.8" />

</androidx.constraintlayout.widget.ConstraintLayout>